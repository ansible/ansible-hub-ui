export class AnsibleRepositoryType {
  description: string;
  name: string;
  pulp_created: string;
  pulp_href: string;
  pulp_labels: any;
  retain_repo_versions: number;

  // gpgkey
  // last_synced_metadata_time
  // latest_version_href
  // remote
  // versions_href
}
